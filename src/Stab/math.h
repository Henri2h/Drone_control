#ifndef MATH_LOOP    // To make sure you don't declare the function more than once by including the header multiple times.
#define MATH_LOOP


float mapValue(float in_val, float in_min, float in_max, float out_min, float out_max);
#include "math.cpp"

#endif